export class Movimiento{
  constructor(concepto, programador, monto, fecha, descripcion, id){
    this.concepto = concepto
    this.programador = programador
    this.fecha = fecha
    this.monto = monto
    this.descripcion = descripcion
    this.id = id
  }
}